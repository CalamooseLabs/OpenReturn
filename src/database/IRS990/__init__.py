"""IRS 990 data layer: schema/seed (sql/) and the repository classes the
OpenReturnDB facade composes. No public class here — import repositories from
``database.IRS990.repositories`` and the facade from ``database``."""
